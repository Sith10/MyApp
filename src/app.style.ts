import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  message: {
    textAlign: 'center',
  },
  container: {
    padding: 5,
  },
  colorText: {
    textAlign: 'center',
  },
  main: {
    flex: 1,
    flexDirection: 'column',
  },
});
